package com.yargisoft.weatherapp.ui.viewmodel

import androidx.lifecycle.ViewModel
import com.yargisoft.weatherapp.POJO.ModelClass

/*
class MainViewModel : ViewModel() {
    private val mainRepo = MainPageRepository()

    fun getCurrentLocation() {
        mainRepo.getCurrentLocation()
    }

    fun isLocationEnabled() {
        mainRepo.isLocationEnabled()
    }

    fun requestPermission() {
        mainRepo.requestPermission()
    }

    fun checkPermission(): Boolean {
        return mainRepo.checkPermission()
    }

    fun fetchCurrentLocationWeather(latitude: String, longitude: String) {
        mainRepo.fetchCurrentLocationWeather(latitude, longitude)
    }

    fun setDataOnViews(body: ModelClass?) {
        mainRepo.setDataOnViews(body)
    }

    fun timeStampToLocalDate(timeStamp: Long): String {
        return mainRepo.timeStampToLocalDate(timeStamp)
    }

    fun kelvinToCelcius(temp: Double): Double {
        return mainRepo.kelvinToCelcius(temp)
    }

    fun getCityWeather(cityName: String) {
        mainRepo.getCityWeather(cityName)
    }

    fun updateUI(id: Int) {
        mainRepo.updateUI(id)
    }
}*/
